# Golden Data — 人工阅读说明

这份文档是给**开发者**看的，用于人工理解 8 个 Golden Customers 分别在表达什么业务情形。

> 它不是运行时数据，也不应被 Agent Runtime 读取。
> `src/eoa/loader.py` 只加载 `data/meta.yaml` 与本目录下的 6 个 `.yaml` 文件，README.md 不在白名单内。

---

## 阅读前提

| 项 | 值 | 来源 |
|---|---|---|
| Reference Time | `2026-08-11T09:00:00+08:00` | `data/meta.yaml` |
| Reference Date | `2026-08-11`（周二） | 由 reference_time 派生 |
| Timezone | `Asia/Shanghai` | `data/meta.yaml` |
| Current User | `rep_li_wei` | `data/meta.yaml` |

**所有"已逾期""几天没联系""未回复"都不是数据里的字段**，而是相对上面这个基准时间算出来的（见 `src/eoa/derive.py`）。改 `meta.yaml` 里的 `reference_time`，这些结论会整体平移。

八个客户全部由 `rep_li_wei`（即 `current_user`）负责。这是刻意的：后续若按负责人过滤候选集，不会有任何 Golden Case 意外掉出范围。

---

## 数据总量

| 实体 | 条数 |
|---|---|
| Customer | 8 |
| Opportunity | 5 |
| Interaction | 13 |
| FollowUpTask | 7 |
| Email | 16 |
| CalendarEvent | 9 |
| **合计** | **58** |

CUST-006 / CUST-007 / CUST-008 没有 Opportunity，CUST-006 / CUST-007 没有 FollowUpTask —— 这些"缺失"本身就是场景的一部分，不是数据不全。

---

## G001 · CUST-001 恒泰精密制造

**当前状态**
Prospect。`OPP-001 恒泰精密 MES 集成一期`，Proposal 阶段，28 万元，预计 8 月 28 日成交。

**关键业务事实**
- `TASK-001 提交更新后的报价明细`，`due_date = 2026-08-11`，`status = open` —— **今天到期**（注意：到期 ≠ 逾期，判定用严格小于）。
- 最近一次接触是 `INT-002`（2026-08-03 电话），距今 **8 天**。
- 电话里我方明确承诺"8 月 11 日前提交报价明细"，`EMAIL-002` 里又书面确认了一次。
- `THREAD-001` 已由我方回复收尾，**没有**未回复来信。

**为什么值得关注**
一个还在推进中的 Proposal，我方公开承诺的交付物今天到期，而且已经 8 天没有任何接触。承诺兑现窗口正在关闭。

**对应哪些数据**
`CUST-001` / `OPP-001` / `INT-001,INT-002` / `TASK-001` / `EMAIL-001,EMAIL-002` / `EVT-001,EVT-002`

---

## G002 · CUST-002 明远零售集团

**当前状态**
Prospect。`OPP-002 全渠道订单中台`，Demo 阶段，36 万元。

**关键业务事实**
- `EMAIL-004`（2026-08-10 收到）提出三个明确的商务问题：实施周期、分期付款、验收标准。
- `THREAD-002` 中在它之后**不存在** `status = sent` 的邮件。
- 存在 `EMAIL-005`，`status = draft`，`sent_at = null` —— **草稿不算已回复**。
- 8 月 5 日刚做完演示（`INT-003`），沟通频率本身正常。

**为什么值得关注**
客户昨天提出了推进评审所必需的问题，我方起草了回复但从未发出。这正是把"已读"当作"已回复"会漏掉的情形 —— 所以 Schema 里没有 `is_read`。

**对应哪些数据**
`CUST-002` / `OPP-002` / `INT-003` / `TASK-002` / `EMAIL-003,EMAIL-004,EMAIL-005` / `EVT-003`

---

## G003 · CUST-003 云汇金融科技

**当前状态**
Prospect。`OPP-003 风控数据平台`，**Negotiation 阶段，50 万元**，预计 8 月 20 日成交。

**关键业务事实**
- 最近一次接触是 `INT-005`（2026-07-30 商务谈判电话），距今 **12 天**；`OPP-003.updated_at` 同样停在 7 月 30 日。
- 卡点是客户法务意见，客户明确表示**没给回复时间**。
- `TASK-003` 在跟这件事，`due_date = 2026-08-14`，尚未到期。
- 预计成交日只剩 9 天。

**为什么值得关注**
金额最大、阶段最靠后的商机，卡在一个没有时间承诺的外部环节上，静默 12 天，而预计成交日迫在眉睫。

> Phase 0B **没有**定义"多少金额算 high value"。`amount_cny = 500000` 是一个具体的编写事实，测试也只断言这个具体值，不断言它是全数据集最大值。

**对应哪些数据**
`CUST-003` / `OPP-003` / `INT-004,INT-005` / `TASK-003` / `EMAIL-006,EMAIL-007` / `EVT-004`

---

## G004 · CUST-004 康和医疗器械

**当前状态**
Prospect。`OPP-004 设备台账系统`，Proposal 阶段，22 万元。

**关键业务事实**
- `EVT-005 方案终审会`，`status = scheduled`，`start_at = 2026-08-12T14:00+08:00` —— **明天下午**，落在 reference time 之后 2 天内。
- 客户方决策层三人参会，会议室已由客户安排（`EMAIL-008`）。
- `TASK-004 准备终审会材料`，`due_date = 2026-08-12`，`in_progress`。
- 这是全数据集中**唯一**一个 `scheduled` 状态的日历事件。

**为什么值得关注**
这是一个反向用例：客户确实处于关键阶段，但会议已经安排好了。正确动作是备料，**不是**再安排一次会议。

**对应哪些数据**
`CUST-004` / `OPP-004` / `INT-006` / `TASK-004` / `EMAIL-008,EMAIL-009` / `EVT-005,EVT-006`

---

## G005 · CUST-005 星海物流

**当前状态**
Prospect。`OPP-005 仓配调度优化`，**Lost**，`actual_close_date = 2026-08-04`。

**关键业务事实**
- `lost_reason = budget_cancelled` —— **结构化字段**，不需要从 `INT-008` 的文字里推断。
- 丢单原因是客户集团取消年度预算，不是方案或竞争问题（`INT-008` / `EMAIL-010`）。
- `TASK-005 归档丢单原因` 已 `done`。
- 没有未回复来信，没有逾期任务。

**为什么值得关注**
反向用例：这个客户不应该出现在常规跟进列表里。商机已终结，且终结原因是外部预算决策，普通销售动作改变不了。明年一季度重新评估是另一件事。

**对应哪些数据**
`CUST-005` / `OPP-005` / `INT-007,INT-008` / `TASK-005` / `EMAIL-010,EMAIL-011` / `EVT-007`

---

## G006 · CUST-006 瑞方智能科技

**当前状态**
`lifecycle_status = customer`（存量客户），`contract_end_date = 2026-08-31` —— **20 天后到期**。

**关键业务事实**
- 该客户**没有任何 Opportunity** —— 续约商机还没建。
- 该客户**没有任何 `scheduled` 日历事件** —— 续约会议还没排。
- 该客户**没有任何 FollowUpTask** —— 续约动作还没被记下来。
- 最近一次接触是 `INT-010`（2026-07-22 季度回顾会），距今 20 天，且会上**明确没有涉及续约安排**。

**为什么值得关注**
唯一的信号来自 `contract_end_date` 这个静态字段。没有任何人主动做了动作 —— 恰恰因为"什么都没发生"，靠邮件、任务、日历这些活动流是发现不了的。三个"不存在"是这个用例的核心，不是数据缺失。

**对应哪些数据**
`CUST-006` / `INT-009,INT-010` / `EMAIL-012` / `EVT-008`（无 Opportunity、无 Task、无 scheduled 事件）

---

## G007 · CUST-007 广盛食品集团

**当前状态**
`lifecycle_status = customer`，`contract_end_date = null`（存量客户，未记录合同到期日 —— 生命周期规则允许这一分支为空）。

**关键业务事实**
- `EMAIL-014`（2026-08-10 收到）提出一个具体的产品功能问题：补货预警阈值能否按单个门店分别设置、能否批量导出。
- `THREAD-007` 中之后**没有** `sent` 邮件，也**没有**草稿。
- 该客户**没有任何 FollowUpTask** —— 这个问题连记录都没被记录下来。
- 客户说明这个问题在内部推广时会被反复问到。

**为什么值得关注**
既是"未回复来信"，也是"客户问题未被登记"。相比 G002（至少写了草稿），这里连承接动作都没有。

> Phase 0B **只表达"客户提了产品问题且尚未回复"**。产品本身能不能做、正确答案是什么，数据里没有，也没有知识库 —— 那是后续阶段的事。

**对应哪些数据**
`CUST-007` / `INT-011` / `EMAIL-013,EMAIL-014`（无 Opportunity、无 Task、无日历事件）

---

## G008 · CUST-008 北岸建材科技

**当前状态**
`lifecycle_status = customer`，`contract_end_date = 2027-03-31`（远未到期）。

**关键业务事实**
- 最近一次接触是 `INT-013`（2026-08-09 季度业务回顾会），距今 **2 天**。
- **没有**逾期任务：`TASK-006` 虽然 `due_date = 2026-08-10` 已过，但 `status = done`；`TASK-007` 到期日是 9 月 4 日。
- **没有**未回复来信：`EMAIL-015` 当天就由 `EMAIL-016` 回复了。
- 回顾会上各项指标达标，客户无遗留问题。
- 没有 Opportunity，也不需要有。

**为什么值得关注**
它**不**值得关注 —— 这就是它存在的意义。这是负向对照组，用来检出"把所有客户都标成高优先级"这种退化行为。`TASK-006` 特意设成"到期日已过但已完成"，用来验证 overdue 判定确实检查了 `status`。

**对应哪些数据**
`CUST-008` / `INT-012,INT-013` / `TASK-006,TASK-007` / `EMAIL-015,EMAIL-016` / `EVT-009`

---

## 编辑这些数据时请注意

1. **时间戳必须加引号。** PyYAML 的隐式 timestamp 解析器会把 `2026-08-11T09:00:00+08:00` 转成**丢掉时区的 naive UTC** datetime。`loader.py` 已经移除了该解析器，但保持加引号仍是最直观的防线。
2. **改一处会牵动多处。** 比如把 `INT-002` 的日期从 8 月 3 日往后挪，G001 的"8 天未联系"就不成立，`test_scenario_coverage.py::test_g001_runtime_facts` 会失败 —— 这是预期行为，不是测试太脆。
3. **不要往这些文件里写答案。** `should_prioritize` / `expected_signals` / `must_not` 只允许出现在 `tests/fixtures/golden_cases.yaml`，`test_business_rules.py::test_golden_yaml_contains_no_expected_answers` 会强制这一点。
4. **改完跑校验：**
   ```bash
   python -m eoa      # 跨实体规则校验
   python -m pytest   # Schema + 业务规则 + 场景覆盖
   ```
